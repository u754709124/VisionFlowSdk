<Window x:Class="Vision.Flow.Demo.DesignerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Vision Flow Designer"
        Width="1280"
        Height="800"
        MinWidth="1100"
        MinHeight="680"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#F4F7FA"
        Closing="OnClosing"
        StateChanged="OnWindowStateChanged">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="52" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#294862" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#102A40" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CloseTitleBarButtonStyle"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C63B4A" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar"
              Grid.Row="0"
              Background="#18324B"
              MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal"
                        Margin="18,0,0,0"
                        VerticalAlignment="Center">
                <Border Width="30"
                        Height="30"
                        CornerRadius="7"
                        Background="#10A372">
                    <Path Width="17"
                          Height="17"
                          Data="M3,4 L8,2 L13,4 L18,2 L18,15 L13,18 L8,15 L3,18 Z M8,2 L8,15 M13,4 L13,18"
                          Stretch="Uniform"
                          Stroke="White"
                          StrokeThickness="1.5"
                          StrokeLineJoin="Round" />
                </Border>
                <StackPanel Margin="10,0,0,0"
                            VerticalAlignment="Center">
                    <TextBlock Text="Vision Flow Designer"
                               Foreground="White"
                               FontSize="14"
                               FontWeight="SemiBold" />
                    <TextBlock Text="SDK WPF Demo"
                               Foreground="#AFC1D1"
                               FontSize="10.5"
                               Margin="0,1,0,0" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource TitleBarButtonStyle}"
                        ToolTip="最小化"
                        Click="OnMinimizeClick">
                    <Path Width="12"
                          Data="M2,8 L14,8"
                          Stroke="White"
                          StrokeThickness="1.4" />
                </Button>
                <Button x:Name="MaximizeButton"
                        Style="{StaticResource TitleBarButtonStyle}"
                        ToolTip="最大化"
                        Click="OnMaximizeClick">
                    <Path Width="12"
                          Height="12"
                          Data="M2,2 L14,2 L14,14 L2,14 Z"
                          Stretch="Uniform"
                          Stroke="White"
                          StrokeThickness="1.3" />
                </Button>
                <Button Style="{StaticResource CloseTitleBarButtonStyle}"
                        ToolTip="关闭"
                        Click="OnCloseClick">
                    <Path Width="12"
                          Height="12"
                          Data="M2,2 L14,14 M14,2 L2,14"
                          Stretch="Uniform"
                          Stroke="White"
                          StrokeThickness="1.4"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round" />
                </Button>
            </StackPanel>
        </Grid>

        <Grid x:Name="DesignerHost" Grid.Row="1" />
    </Grid>
</Window>
